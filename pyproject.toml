[project]
name = "hospitalmanagement"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mustafa Taj", email = "mustafataj2025@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
hospitalmanagement = "hospitalmanagement:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
